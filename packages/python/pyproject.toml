[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cn-area"
version = "1.0.0"
description = "中国行政区划数据包（Python）"
requires-python = ">=3.10"
license = "MIT"
keywords = ["china", "administrative", "area", "region"]
